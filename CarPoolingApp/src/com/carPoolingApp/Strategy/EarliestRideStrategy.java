package com.carPoolingApp.Strategy;

public class EarliestRideStrategy implements IRideSelectionStrategy {
    @Override
    public void select(String name, String origin, String destination) {
        //Get the rides with Origin and destination and then get the earliest end time
        // UserOfferedRideRepository.offeredRideMap.

        //Add to userRideTakenRepository
    }
}
